configuration:
  anthropic_api_key:
    name: Anthropic API Key
    description: >-
      API key for Claude Code. Get one at https://console.anthropic.com/.
      Leave empty to configure providers manually through the T3 web UI.
network:
  3773/tcp: T3 Code web interface
